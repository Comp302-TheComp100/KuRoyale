package com.kuroyale.service;

import com.kuroyale.model.ArenaLayout;
import com.kuroyale.model.User;

/**
 * Orchestrates gameplay logic.
 * Currently a placeholder/helper for future expansion of complex battle logic.
 * Most logic is currently in GameState and BattleController for MVP.
 */
public class GameplayService {

    public GameplayService() {
    }

    public void startBattle(User user, ArenaLayout layout) {
        // Setup battle configuration if needed
    }

    // Future: Add methods for complex interactions, damage calculations, etc.
}
